<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="is.hi.recipeapp.hugbv2">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SearchActivity"
            android:label="@string/title_activity_search" />
        <activity
            android:name=".SignupActivity"
            android:label="Sign up"/>
        <activity android:name=".AdminLogin" />
        <activity android:name=".AdminPage" />
        <activity
            android:name=".ui.MyShoppingList"
            android:label="MyShoppingList"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AboutActivity"
            android:label="Recipe Info"/>
        <activity
            android:name=".MyProfile"
            android:label="My profile"/>
        <activity
            android:name=".ui.MyFavoriteRecipe"
            android:label="Favorite Recipes"/>
        <activity
            android:name=".ui.MyWeekMenu"
            android:label="My Week Menu"/>
        <activity android:name=".ui.MyDayDetail"></activity>
    </application>

</manifest>